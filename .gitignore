# superpowers / specs / ferramentas de agente — nunca versionar
docs/superpowers/
docs/specs/
.claude/

node_modules/
dist/
*.log
*.deb

# Bun-only project — npm lockfile must not come back
package-lock.json
