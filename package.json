{
  "name": "ttm",
  "version": "0.1.0",
  "description": "Pick a terminal theme and see it live in your own window",
  "type": "module",
  "bin": { "ttm": "./src/cli.ts" },
  "files": ["src", "themes", "README.md", "LICENSE"],
  "license": "MIT",
  "scripts": {
    "test": "bun test",
    "build": "bun build --compile src/cli.ts --outfile dist/ttm",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
